{
  "name": "cogs-plugin-buzzshot",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@clockworkdog/cogs-client-react": "1",
    "base-64": "^1.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "cross-env BROWSER=scripts/openSimulator.js PORT=3001 react-scripts start",
    "build-content": "cross-env BUILD_PATH=build/co.buzzshot.groups react-scripts build",
    "build-manifest": "mkdir -p build/co.buzzshot.groups && cp -fv cogs-plugin-manifest.json build/co.buzzshot.groups/cogs-plugin-manifest.json",
    "build": "yarn build-content && yarn build-manifest",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "./",
  "devDependencies": {
    "@types/base-64": "^1.0.0",
    "@types/node": "14",
    "@types/react": "18",
    "@types/react-dom": "18",
    "cross-env": "7",
    "typescript": "4"
  }
}
